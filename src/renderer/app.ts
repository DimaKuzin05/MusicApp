// отвечаетЗА: одна строка — запуск AppController
// Точка входа UI: создание 10 объектов, инициализация

import { AppController } from './AppController';

// Вся логика старта — в классе AppController (метод start)
AppController.start();
